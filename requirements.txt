# The Agentic Lab: supported version ranges.
# Exact pins live in requirements.lock.txt after build testing.
# Python 3.11 or later.
#
# Upper bounds are deliberate: the printed listings are tested against
# these majors. pandas is capped below 3.0 because 3.0 changed the
# default string dtype and made copy-on-write mandatory, which changes
# the result of the dtype=str instrument reads in Chapter 5.

anthropic>=0.40,<2.0
openai>=2.0,<4.0
pydantic>=2.7,<3.0
python-dotenv>=1.0
httpx>=0.27

pandas>=2.2,<3.0
numpy>=1.26,<3.0
# pandera.pandas is the modern namespace and Chapter 5 prints it. The
# floor is the version that exposes it; it must never pull pandas past
# the cap above.
pandera>=0.20,<1.0
biopython>=1.83

pytest>=8.0,<10.0
pyyaml>=6.0

# tools/verify_references.py only. Title comparison against what Crossref
# returns, so a reference whose title drifted from the record is a score
# rather than an argument. No build imports it and no test needs it.
rapidfuzz>=3.0,<4.0

# Introduced in Chapter 3, after the raw loop
langgraph>=1.0,<2.0
