# The Agentic Lab: minimum versions.
# Exact pins live in requirements.lock.txt after build testing.
# Python 3.11 or later.

anthropic>=0.40
openai>=1.50
pydantic>=2.7
python-dotenv>=1.0
httpx>=0.27

pandas>=2.2
numpy>=1.26
biopython>=1.83

pytest>=8.0
pyyaml>=6.0

# Introduced in Chapter 3, after the raw loop
langgraph>=0.2
