{
  "design": "commitment_after_data.yaml",
  "expected_failure": "commitment_after_data",
  "breaks": "committed_at falls after the earliest reading in fixtures/results, so the model was chosen once the surfaces existed. Nothing else in the study would record that.",
  "stock_mM": 1000.0,
  "transfer_uL": 100.0,
  "max_solvent_pct": 0.5,
  "review_note": "Reviewed against a 1 M stock so the solvent check, which the printed design trips at 100 mM, does not fire first and mask the failure this design is testing."
}
